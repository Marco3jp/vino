export default class overhaulType {
    constructor() {
        /**
         * さよならを教えてやひぐらしのなく頃にのような
         *     サウンドノベル式のスタイルにするか判断する変数(実装時期未定)
         * @type {boolean}
         */
        this.fullInnerBox = false;
        /**
         * 名前が入る箱を用意するか判断する変数(実装時期未定)
         * @type {boolean}
         */
        this.nameBox = false;
        /**
         * タイトル画面の有無を判断する変数(falseの場合の実装は後回し)
         * @type {boolean}
         */
        this.titleScreen = true;
    }
}